query PaginatedPost($options: PaginatedPostInput!) {
  paginatedPosts(options: $options) {
    id
    postDetails {
      id
      body
      updatedAt
    }
    poster {
      id
      verified
      username
      fullName
      profileImg
    }
  }
}
