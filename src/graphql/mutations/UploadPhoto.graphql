mutation UploadPhoto($options: UploadImgInput!) {
  uploadPhoto(options: $options) {
    error {
      message
    }
    photo {
      id
      createdAt
      updatedAt
      src
    }
    signedRequest
  }
}
