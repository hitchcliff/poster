mutation UploadPhoto($values: UploadPhotoInput!) {
  uploadPhoto(values: $values) {
    error {
      message
    }
    photo {
      id
      createdAt
      updatedAt
      src
    }
    url
    signedRequest
  }
}
